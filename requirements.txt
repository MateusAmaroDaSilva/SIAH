# Servidor da API
fastapi                # Construir a API principal
uvicorn[standard]      # Rodar o servidor da API com hot reload

# Banco de dados e modelos
sqlalchemy             # Integração com banco relacional
pydantic               # Validar dados recebidos/enviados pela API
python-multipart       # Para lidar com uploads/formulários multipart

# Comunicação externa
requests               # Enviar dados do captura.py para a API
supabase               # Acesso ao Supabase (storage + banco)

# Processamento de imagens
opencv-python          # Capturar vídeo e processar imagens
numpy                  # Manipulação de arrays, cálculos de embeddings
face_recognition       # Reconhecimento facial robusto
dlib                   # Dependência do face_recognition

# Templates (se ainda usa)
jinja2                 # Gerar páginas HTML dinâmicas
